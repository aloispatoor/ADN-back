{% extends 'blogbase.html.twig' %}

{% block title %}{{ user.username }}{% endblock %}

{% block body %}
    <section id="userProfile">
        <h2>{{ user.username }}</h2>
        <div class="flexbox">
            <article id="userInfo">
                <h3>informations</h3>
                <div class="flexbox">
                    <div id="user-avatar">
                        {% if user.avatarFileName %}
                            <img src={{ vich_uploader_asset(user) }} alt={{user.username}}>
                        {% else %}
                            <img src={{ asset('images/avatar_generic.png') }} alt="Avatar générique">
                        {% endif %}
                    </div>
                    <div class="user-text">
                        
                    </div>
                </div>
            </article>
            <article id="userInfoBis">
                <p><span class="bold">Prénom : {{ user.name }}</span></p>
                <p><span class="bold">Ville : {{ user.city }}</span></p>
                {% for gender in genders %}
                    <p><span class="bold">Genre : {{ gender.label }}</span></p>
                    {% if gender.id is null %}
                        <p><span class="bold">Genre : --</span></p>
                    {% endif %}
                {% endfor %}
                <p><span class="bold">Pronoms : {{ user.pronouns }}</span></p>
            </article>
        </div>
        {% if user.articles is empty %}

        {% else %}
            <article id="user-articles">
                <h3>articles</h3>
                {% for article in articles %}
                    <div class="user-article">
                        <p>{{ article.title }}</p>
                        <div class="button-container">
                            <a href="{{path('app_article_single', {id: article.id})}}" class="button">Voir</a>
                        </div>
                {% endfor %}
            </article>
        {% endif %}
    </section>
{% endblock %}